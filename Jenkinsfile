pipeline {
    agent any // Run on any available agent

    stages {
        stage('Test Configuration') {
            steps {
                echo 'Jenkins configuration is working!'
            }
        }
    }
}
